[phases.setup]
nixPkgs = ["jdk17", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests -q"]

[start]
cmd = "java -Dserver.port=${PORT:-8080} -jar target/ezworks-backend-0.1.0-SNAPSHOT.jar"
